% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_interview_time.R
\name{add_interview_time}
\alias{add_interview_time}
\title{Calculate and add interview duration in minutes}
\usage{
add_interview_time(
  .dataset,
  start_col = "interview_start",
  end_col = "interview_end",
  new_col = "interview_duration_mins"
)
}
\arguments{
\item{.dataset}{A data frame containing the start and end datetime columns.}

\item{start_col}{Character; name of the column containing start datetimes.
Defaults to `"interview_start"`.}

\item{end_col}{Character; name of the column containing end datetimes.
Defaults to `"interview_end"`.}

\item{new_col}{Character; name of the new duration column to create.
Defaults to `"interview_duration_mins"`.}
}
\value{
The input data frame with a new numeric column containing interview
  duration in minutes (to 2 decimal places).
}
\description{
Computes the difference in time in minutes (rounded to 2 decimal places)
between a start datetime column and an end datetime column, and adds the
result as a new column in the dataset.

Both columns must contain POSIXct, POSIXlt, or character datetime values
with a time component (e.g. `"2025-10-16 14:32:00"`).
}

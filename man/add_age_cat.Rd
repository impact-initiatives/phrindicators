% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_age_cat.R
\name{add_age_cat}
\alias{add_age_cat}
\title{Add Age Categories}
\usage{
add_age_cat(.dataset, age_years_col)
}
\arguments{
\item{.dataset}{A data frame or tibble containing the required column.}

\item{age_years_col}{Column name (character) indicating the age of individuals in years.}
}
\value{
A data frame or tibble with the following new column:
* **age_cat**: A factor column indicating the 5-year age category of each individual.
}
\description{
Categorizes individuals into 5-year age bins based on their age in years.
}
\details{
The function adds a new factor column `age_cat` to the dataset, where age ranges are calculated in 5-year intervals (e.g., 0-4 years, 5-9 years, etc.) up to a maximum of 120 years. The bins are created as:
 - "0-4", "5-9", ..., "120-124".
Any age values outside the range [0, 125) are treated as missing.
}
\examples{
# Example dataset
df <- data.frame(
  age_years = c(0, 3, 7, 10, 45, 87, 122, NA)
)

# Add age categories
df_result <- add_age_cat(
  .dataset = df,
  age_years_col = "age_years"
)
print(df_result)

}
